version: '3.8'
services:
  frontend:
    build: ./client
    ports:
      - "80:80"
    environment:
      - REACT_APP_API_URL=https://your-backend.onrender.com

  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=10000
      - FLASK_ENV=production